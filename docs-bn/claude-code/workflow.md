# Claude Code ডেভেলপমেন্ট ওয়ার্কফ্লো

JanVayu তৈরিতে ব্যবহৃত দৈনিক ওয়ার্কফ্লো।

---

## ফিচার ডেভেলপমেন্ট

### ধাপ 1: ফিচার সংজ্ঞায়িত করুন
```
> একটি anomaly detection ব্যানার যোগ করুন যা দিল্লি, মুম্বাই, কলকাতা,
> চেন্নাই এবং বেঙ্গালুরুর জন্য PM2.5-এর মৌসুমী বেসলাইনের সাথে তুলনা করে
```

### ধাপ 2: Claude কোডবেস পড়ে
- `index.html` — বিদ্যমান UI প্যাটার্ন
- বিদ্যমান Netlify Functions — কোড স্টাইল
- `package.json` — উপলব্ধ ডিপেন্ডেন্সি

### ধাপ 3: Claude ইমপ্লিমেন্ট করে
- `netlify/functions/anomaly-check.mjs` তৈরি করে
- `index.html`-এ ব্যানার HTML/CSS/JS যোগ করে

### ধাপ 4: রিভিউ ও সংশোধন
```
> ব্রিফটি খুব লম্বা হয়েছে। Gemini আউটপুট 400 টোকেনে সীমিত করুন।
```

### ধাপ 5: কমিট ও PR
Claude `git add`, `git commit`, `git push`, এবং `gh pr create` চালায়।

---

## বাগ ফিক্স ওয়ার্কফ্লো

```
> anomaly-check ফাংশন WAQI ডাউন থাকলে 500 রিটার্ন করছে।
> এটি ক্যাশড ডেটার ভিত্তিতে 200 রিটার্ন করা উচিত।
```

Claude ন্যূনতম পরিবর্তন করে — পুরো ফাংশনটি আবার লেখে না।

---

## কার্যকর প্রম্পট প্যাটার্ন

### নতুন ফিচারের জন্য
```
[ফিচার] যোগ করুন। প্রয়োজনীয়তা:
- [সীমাবদ্ধতা 1]
- [সীমাবদ্ধতা 2]
[রেফারেন্স ফাইল]-এ বিদ্যমান প্যাটার্ন অনুসরণ করুন।
```

### বাগ ফিক্সের জন্য
```
[ফাংশন] [অবস্থায়] [ত্রুটি] রিটার্ন করছে।
প্রত্যাশিত: [সঠিক আচরণ]।
ন্যূনতম পরিবর্তন করুন।
```
